// SoleBlessing — OAuth stub (Manus removed)
// Admin auth is handled client-side via VITE_ADMIN_PW
import type { Express } from "express";
export function registerOAuthRoutes(_app: Express) {
  // No OAuth routes needed
}
